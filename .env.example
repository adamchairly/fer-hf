# ── MSSQL ──
MSSQL_SA_PASSWORD=YourStrong!Passw0rd
MSSQL_DATABASE=FerHf
MSSQL_PORT=1433

# ── Backend ──
ASPNETCORE_ENVIRONMENT=Development
BACKEND_PORT=8080

# ── Azure Blob Storage ──
# Local Docker Compose: leave empty to use Azurite defaults
# Azure App Service: set to your real Azure Storage connection string
AZURE_BLOB_CONNECTION_STRING=
AZURE_BLOB_CONTAINER=fer-hf
AZURITE_BLOB_PORT=10000
# Public URL the browser uses to fetch blobs
# Local Docker Compose: leave empty (defaults to http://localhost:10000/devstoreaccount1)
# Production: leave empty (the real Azure Blob URL from the SDK is already correct)
AZURE_BLOB_PUBLIC_BASE_URL=

# ── Frontend ──
FRONTEND_PORT=3000
# Local Docker Compose: leave empty (nginx proxies /api to backend)
# Vercel: set to your Azure App Service URL (e.g. https://ferhf-api.azurewebsites.net)
VITE_API_BASE_URL=

# ── CORS ──
# Local Docker Compose: http://localhost:3000
# Production: your Vercel domain (e.g. https://ferhf.vercel.app)
ALLOWED_ORIGINS=http://localhost:3000
